// Checker 오류 데모: 변수 선언 시 자기 자신을 초기화식에서 참조
// 파일 전체를 먼저 검사(Checker)한 뒤에 실행하므로, 아래 print는 실행되지 않는다.
var b = 5;
print b; // expect: (실행되지 않음 - Checker 단계에서 전체 검사 후 오류로 중단)
var a = a + 1; // expect error: [line 5] Type : IDENTIFIER, Lexeme : a, literal : nil, message : 자신의 초기화식에서 지역변수를 읽을 수 없습니다: 'a'
